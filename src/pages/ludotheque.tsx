import {
  ChakraProvider,
  theme,
  Center
} from "@chakra-ui/react";

const ToyLibraryPage = () => (
  <ChakraProvider theme={theme}>
    <Center>En cours de travaux</Center>
  </ChakraProvider>
);

export default ToyLibraryPage;
